<view class="page"><text class="eyebrow">你的交付</text><view class="title">现场心意已送达</view><view wx:if="{{loading}}" class="card muted">正在安全读取交付文件…</view><view wx:elif="{{error}}" class="card"><text class="error">{{error}}</text></view><view wx:elif="{{isVideo}}" class="media-card"><video class="video" src="{{filePath}}" controls autoplay /></view><view wx:else class="media-card"><image class="image" src="{{filePath}}" mode="widthFix" /></view><view class="card muted">交付文件通过已登录账户读取，不会展示或复制原始交付链接。</view></view>
