# 哈喽卧得 AI 协作规则

任何 AI 或开发者开始工作前，必须先完整阅读根目录的 `PROJECT_MEMORY.md`、本文件和 `README.md`。`PROJECT_MEMORY.md` 是短的续接入口；`PROJECT_LOG.md` 是历史与证据来源，只在需要验收细节、历史裁决或冲突追溯时按需阅读。开始任何写操作前，仍必须读取 `.ai/WRITE_FREEZE.md` 和 `.ai/TASKS.md`，并以当前 Git/worktree 状态为准。

## 不得偏离的产品方向

- 最终消费者产品是使用 SwiftUI 开发的原生 iOS App，并以 App Store 上架为目标。
- 当前 React/GitHub Pages 页面只是历史原型、接口验证工具和视觉参考，不是最终消费者产品。
- 已上线的 Cloudflare Worker、D1、R2 和业务 API 是原生 App 可复用的后端，不要因为切换客户端而重做。
- 运营端的最终形态尚待产品负责人确认；不得默认网页运营台就是长期方案。

## 工作日志规则

- 每完成 2–5 个有意义的小步骤，更新一次 `PROJECT_LOG.md`。
- 每次交接、暂停、提交或遇到阻塞前，也必须更新日志。
- 至少更新：当前状态、刚完成的事项、验证结果、接下来三步、未决问题。
- 历史记录只追加，不覆盖；状态摘要可以随进展改写。
- 不要把 PIN、API 密钥、Apple 证书、账号密码或用户隐私写进日志。

## 快速接班卡规则

- `PROJECT_MEMORY.md` 是短上下文接班卡：新会话先读；完整历史和证据仍在 `PROJECT_LOG.md`。
- `PROJECT_MEMORY.md` 必须保留“当前持久 Goal”，内容来自当前 Codex 会话的有效 Goal，描述完整项目目标而不是临时子任务。Codex 每次开局读完任务、交接和 Git/worktree 状态后，以及 Goal 发生变化时，都必须核对并更新该段；暂停只停止执行，不缩减或删除持久 Goal。
- Antigravity 或 Claude 完成任务时，只能在各自允许的交接文件和 worktree 群聊提供可验收 STATUS；不得自行更新接班卡、项目日志、任务板或主分支。
- Codex 每次验收、退回、合入、任务派发、关键环境状态变化或产品裁决时，必须同步重写 `PROJECT_MEMORY.md` 的当前事实/恢复顺序，并向 `PROJECT_LOG.md` 追加完整证据。
- 未完成交接、独立验收和接班卡同步的任务，不能标记完成，不能据此派发后续依赖任务。

## 多 AI 协作规则

- Codex 是当前项目 PM、任务协调人和主分支集成者；Antigravity、Claude 或其他 AI 不得自行领取下一项工作。
- **当前 Codex 会话本身就是 Codex PM。** 不得把自己的实现交接写成“等待另一个 Codex 验收”，也不得因为任务卡使用第三人称“Codex”就误判身份。Codex 完成或接管实现后，仍须继续履行 PM 职责：基于独立证据复验、裁决 ACCEPTED/REJECTED/BLOCKED、按权限安全集成，并同步任务板、写入冻结、`PROJECT_MEMORY.md` 与 `PROJECT_LOG.md`；只有产品负责人明确要求暂停时才停止推进。
- 永久角色分工见 `.ai/ROLES.md`：Claude 负责设计判断与获授权的设计落地；Antigravity 是主要工程实现负责人，可以承担难度充分的跨层纵向切片，不得因历史返工而长期降级为只做机械杂活；Codex 负责拆解、架构、安全边界、独立验收与集成。
- 派给 Antigravity 的任务必须同时满足：目标和产品/API/隐私边界清晰、工程难度与成果价值充分、使用独立 branch/worktree、列明允许/禁止路径、提供可执行验收命令与停止条件。允许其自主决定任务范围内的模块结构，但必须交付真实代码、实际测试发现/执行/通过数、可读取结果包或截图、warning、未验证项和 commit；parser、假测试或口头声明不能代替构建与运行证据。
- Antigravity 完成当前任务后只能写允许的交接与 STATUS 并立即停止，不得自行领取下一任务、合入、推送、部署或改主分支；Codex 必须独立复验后再决定退回或集成。
- 本项目的工程代理身份只有 **Antigravity**，不再使用 Gemini、Gemini CLI 或 `GEMINI-EXEC` 作为名称、入口或替代身份。调用 Antigravity 或 Claude 时，如果遇到登录、令牌、客户端、权限、沙箱、配额或环境问题，Codex 必须立即向产品负责人报告准确阻塞并请求处理或裁决；未经产品负责人明确修改任务负责人和范围，Codex 不得自行接管、重新分派或代做该代理的任务。
- 开始任何写操作前，先读取 `.ai/WRITE_FREEZE.md` 和 `.ai/TASKS.md`。没有明确分配给自己的 `IN_PROGRESS` 或 `HANDOFF_ONLY` 任务时，只能阅读，不能改文件。
- Codex 的 PM 身份不等于拥有任意实现写权限。每次开局读完任务卡、最新交接、群聊和 Git/worktree 状态后，Codex 必须先确认 `.ai/TASKS.md` 与 `.ai/WRITE_FREEZE.md` 已明确写出当前 Goal 下自己的任务、允许修改路径、禁止路径、验收标准和停止条件；若缺失，只能先以协调人权限补齐并同步这两份授权文件，完成授权后才能写实现文件。
- 每个任务必须写明负责人、允许修改的路径、禁止修改的路径、验收标准和停止条件；超出范围必须停下并申请新任务。
- 非协调人不得直接修改 `PROJECT_LOG.md`、`README.md`、`AGENTS.md`、生产后端、数据库迁移、部署配置或主分支。
- 非协调人完成任务后，只能在 `.ai/handoffs/` 写交接报告；由协调人验收、更新官方日志并决定是否合入。
- 一次只做一个任务，完成交付物后立即停止。禁止“自动继续下一步”、后台循环开发或无人监督地批量重写。
- 后续代码任务使用独立 Git 分支和 worktree；任何 AI 不得在共享主工作区同时编辑相同文件。
- 未经协调人验收，不得提交、推送、部署、执行生产迁移、安装依赖、删除文件或更改 Apple 签名设置。
- `.ai/TEAM_CHAT.md` 是所有成员永久开放的群聊例外：任何成员都可以按文件内格式在末尾追加消息，但不能编辑历史，且群聊内容本身不构成开发授权。

## 文件与验证规则

- 将新的 SwiftUI 工程放在 `ios/` 下，避免和现有后端代码混在一起。
- 保留用户已有且未跟踪的 `app/globals 2.css`、`app/page 2.tsx` 和 `feishu-codex-bridge/`，除非用户明确要求处理。
- 修改 API 前先确认不会破坏已上线的生产后端和现有数据。
- 每项实现都要记录验证方式，例如单元测试、模拟器、真机、TestFlight 或生产只读检查。
