# 多 AI 写入冻结

状态：`ACTIVE`

冻结原因：Antigravity 在未领取任务、未等待 UI 设计和未经过验收的情况下，一次性生成了完整 `ios/` 候选工程。当前需要先完成交接和代码审查，避免其他 AI 继续叠加修改。

## 当前规则

- Codex 作为 PM/协调人可以进行只读审计，并修改协调文档、项目日志和验收结果。
- Antigravity 只能执行 `.ai/TASKS.md` 中的 `AG-001`，仅写指定交接文件。
- Claude 只能执行 `.ai/TASKS.md` 中的 `CL-001`，只读审查源码并写指定审查报告。
- 其他 AI 只能阅读，不得写任何文件。
- 任何代理不得继续修改 `ios/`、后端、部署配置或 Git 历史。

冻结只有在 Codex 完成审查并在本文件中将状态改为 `LIFTED` 后才解除。用户口头要求某个新功能，不等于自动解除冻结；先由协调人创建任务卡。
